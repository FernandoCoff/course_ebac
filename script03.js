// Em relação as funções, optei por usar a sintaxe das Arrows Functions
const soma = (a,b)=> a+b

const subtracao = (a,b)=> a-b 

const calcularArea = (raio)=> (raio*raio)*3.14159

const saudacao = (nome) =>{
    console.log(`Olá, ${nome}`)
}